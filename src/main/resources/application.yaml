spring:
  profiles:
    active:
      - dev
    group:
      local:
        - local
      dev:
        - dev
      test:
        - test
      ops:
        - ops
    include:
      - jwt
      - oauth
      - rabbitmq
      - firebase